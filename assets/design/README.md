# Design sources

Working artwork, not shipped with the extension. The toolbar/store icons in
`icons/` (16/32/48/128 px) are exported from `icon-source.png` (1254x1254).

Kept out of `icons/` so it never lands in a packaged build.
